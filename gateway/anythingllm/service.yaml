apiVersion: v1
kind: Service
metadata:
  name: anythingllm
  namespace: anythingllm
  labels:
    app.kubernetes.io/name: anythingllm
    app.kubernetes.io/component: ui
spec:
  selector:
    app: anythingllm
  ports:
    - name: http
      protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
