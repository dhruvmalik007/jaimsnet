# Infisical Kubernetes Operator — sync Redis secrets into the gateway namespace.
# projectId, envSlug and hostAPI are managed centrally in:
#   iac/k8s/infisical-config.yaml (ConfigMap: jaimsnet-infisical-config in infisical ns)
# Do NOT hardcode projectId here. Update the ConfigMap when rotating credentials.
apiVersion: secrets.infisical.com/v1alpha1
kind: InfisicalSecret
metadata:
  name: redis-secrets-sync
  namespace: gateway
spec:
  hostAPI: https://app.infisical.com
  resyncInterval: 60
  authentication:
    universalAuth:
      credentialsRef:
        secretName: universal-auth-credentials
        secretNamespace: infisical
      secretsScope:
        projectSlug: "jaimsnet"
        envSlug: "prod"
        secretsPath: "/gateway/redis"
  managedSecretReference:
    secretName: redis-secrets
    secretNamespace: gateway
    creationPolicy: "Orphan"
